# =============================================================
# requirements.txt — Smart Agriculture Backend Dependencies
# Install with: pip install -r requirements.txt
# Compatible with Python 3.13+
# =============================================================

# ── Web Framework ─────────────────────────────────────────────
fastapi==0.115.5
uvicorn[standard]==0.32.1

# ── MQTT Client ───────────────────────────────────────────────
paho-mqtt==2.0.0

# ── Data Validation ───────────────────────────────────────────
pydantic==2.10.3
pydantic-settings==2.6.1

# ── Database ──────────────────────────────────────────────────
motor==3.6.0
pymongo==4.9.2                 # Must be <4.10 — required by motor 3.6.0

# ── HTTP Client (Weather API) ─────────────────────────────────
httpx==0.27.0                  # Async HTTP client for OpenWeatherMap API

# ── Machine Learning ──────────────────────────────────────────
scikit-learn==1.5.2            # Random Forest, preprocessing, metrics
numpy==2.1.3                   # Numerical computing
pandas==2.2.3                  # Data manipulation and CSV loading
joblib==1.4.2                  # Model serialization (.joblib files)

# Deep Learning (Phase 8 — Advanced Models)
torch==2.10.0                  # SwiFT + TTL custom PyTorch models (CPU build)
pytorch-tabnet==4.1.0          # TabNet Classifier for soil + fertilizer
lime==0.2.0.1                  # LIME local explanations for TabNet XAI
shap==0.46.0                   # SHAP global feature importance

# ── Phase 9: Gemini AI, PDF generation, class-imbalance correction ──
google-genai>=1.0.0               # Gen AI SDK — advice_service uses `from google import genai` + genai.Client()
xhtml2pdf==0.2.16                 # Pure-Python PDF (no GTK+, Windows-compatible)
imbalanced-learn>=0.12.0          # SMOTE for class-imbalance in ML training
jinja2>=3.1.0                     # HTML template rendering for PDF reports

# ── Utilities ─────────────────────────────────────────────────
python-dotenv==1.0.1
python-dateutil==2.9.0
