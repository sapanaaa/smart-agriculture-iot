# Force shell scripts to always use LF line endings, even on Windows.
# Prevents the "exec ...: no such file or directory" error inside Linux containers.
*.sh text eol=lf
docker-entrypoint.sh text eol=lf
